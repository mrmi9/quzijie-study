# 微信开发者工具与真机验收清单

## 2026-07-15 微信云托管迁移记录

- [x] Prisma 数据源切换为 MySQL，并生成独立 MySQL 初始迁移；旧 PostgreSQL 迁移已归档。
- [x] 小程序发布配置切换为 `prod-d4gnnimmh1d0677fc / express-tfts`，请求层改用 `wx.cloud.callContainer`。
- [x] 云托管登录改用平台注入的 OpenID，不再要求 AppSecret、JWT 和刷新令牌。
- [x] MySQL 8 真实集成测试通过：迁移、500 题导入、云身份登录、C/C++ 练习、408 并发与到期交卷共 8 项通过。
- [x] 最终 Docker 镜像以非 root 用户启动，自动迁移和空库导入成功，`/health`、`/ready` 均返回 `ok`。
- [x] 已重置暴露过的云数据库密码，并由微信云托管向 `express-tfts` 注入新值。
- [x] 本项目已部署到微信云托管；公网 `/health`、`/ready` 与 MySQL 状态均为 `ok`。
- [ ] 上传 `2.1.0` 体验版并完成 Android 全流程及 iOS 核心流程真机验收。

## 自动门禁

- [x] 2026-07-15 执行 `npm run verify:miniprogram`、`npm run verify:server`、MySQL 8 下的 `npm run verify:integration` 和 `npm run check:release`：500 题、18 页面、9 项服务端测试、8 项真实 MySQL API 闭环及云托管发布门禁全部通过。
- [ ] 微信开发者工具执行“清缓存并编译”，控制台无错误或未处理 Promise。
- [ ] 在至少一台 iOS 和一台 Android 真机预览，检查常见窄屏、长屏和底部安全区。

## 登录、主页与导航

- [ ] 清空缓存进入任一业务页会跳登录，演示登录后回到原入口。
- [ ] TabBar 的首页、错题、收藏、我的可切换，返回时数据会刷新；窄屏和底部安全区下四项均完整显示。
- [ ] 首页显示 500 题进度、今日答题、正确率、错题、收藏和五个模块卡片。
- [ ] 首页不再显示重复的“账户与数据”入口；“我的”显示账户创建时间及今日答题、累计答题、正确率、题库进度。
- [ ] “我的”中的错题本、我的收藏切换到对应 Tab，408 考试记录和隐私说明进入对应页面，错题与收藏数量正确。
- [ ] “我的”下拉刷新后账户和学习概览独立更新；任一区域加载失败时可单独重试，退出与永久删除仍可操作。
- [ ] 未登录选择“我的”进入登录，登录后返回“我的”；退出或永久删除后进入登录页。
- [ ] C/C++ 旧入口 `/modules/cpp/pages/home/index` 仍可直接打开。
- [ ] Linux/操作系统、计网/STL 会先进入方向选择，两个方向进度独立。
- [ ] 首页普通未完成会话和 408 未完成试卷均能继续。

## 七学科普通练习

- [ ] 对 `cpp/linux/os/ds/network/stl/co` 分别创建章节与随机练习。
- [ ] 分别测试 5、10、20 题；章节不足所选题量时取全部且不重复。
- [ ] 单选和判断只能保留一个选择，多选重复点击可取消；未选择时确认禁用。
- [ ] 提交成功后选项锁定，正误、正确答案和解析正确显示。
- [ ] 最后一题完成后进入结果页，题数、正确数、错误数、正确率和章节表现一致。
- [ ] 代码片段在窄屏可横向滚动；若加入题图，能预览缩放且替代说明可见。
- [ ] 创建新普通练习后，旧会话提示 abandoned，首页只显示新会话。

## 网络、登录失效与幂等

- [ ] 普通题提交时模拟断网/超时/5xx，选择不丢失；恢复后重试只计一次。
- [ ] 答题、收藏和恢复时模拟 401，登录后返回原页且状态不丢失。
- [ ] 空题库、空错题、空收藏均显示明确空状态，不进入空白答题页。
- [ ] 将仓储切为 API 后所有页面仍走相同流程，页面源码中没有 `wx.request`。
- [ ] 微信云托管模式连续登录两次得到同一业务用户；日志、客户端包和 Git 中没有 AppSecret、OpenID 或 UnionID 的值。
- [ ] 缺少可信云身份头的请求返回 401；永久删除账户后原身份的业务请求返回 401，再次微信登录创建无历史数据的新账户。

## 错题、掌握与收藏

- [ ] 任一学科普通答错后进入该学科和全局未掌握错题。
- [ ] 错题重做答对后改为已掌握且历史保留；再次答错后恢复未掌握。
- [ ] 全局错题按七学科及掌握状态筛选准确。
- [ ] 答题反馈页收藏、模块收藏页取消和全局收藏数量同步。
- [ ] 全局收藏“全部”标签在加载、失败或空列表时禁用重练；存在收藏时显示并可点击“重练全部收藏”。
- [ ] 全局收藏题量页默认选择“全部”，并可切换 5、10、20；收藏数不足固定题量时取现有全部，题目 ID 无重复。
- [ ] 至少收藏两个学科的题目后统一重练，题目随机混排且每题显示正确学科标签；待答视图不显示正确答案或解析。
- [ ] 全局收藏练习可使用上一题回顾；退出再进入后恢复到首道未答题，已提交答案、未提交本地草稿和收藏状态均保持一致。
- [ ] 完成后总题数、正确数、错误数和正确率一致；各学科表现按注册顺序显示，章节表现均带正确学科名称。
- [ ] 全局结果页“再练一组”返回全局收藏题量页，“查看错题”进入全局错题页，“返回学习首页”进入总首页。
- [ ] 全局错题仍需选定学科后发起重练；单学科收藏重练入口与原流程保持不变。

## 积分、排行榜与成就

- [ ] 首次答错一道新题显示 `积分 +2`；之后首次答对同题显示 `积分 +8`，首次直接答对显示 `积分 +10`。
- [ ] 已掌握题次日答对显示 `积分 +1`；同一天重复答对不重复加复习分，每天超过 20 道复习题后不再加分。
- [ ] 同一道题先在普通练习答对、再在 408 答对时不会再次获得首次作答/首次答对积分；重复提交和重复交卷奖励完全一致。
- [ ] 新成就以带对应徽章的弹窗逐个展示；重新进入已提交普通题不重复弹窗，408 报告同设备只展示一次奖励。
- [ ] “我的”显示默认公开身份、总积分、日/周/总榜排名、已佩戴称号和已解锁数量；积分档案失败不影响学习概览与账户操作。
- [ ] 昵称允许 2–12 位中文、字母、数字和下划线；全角字符会规范化，联系方式、保留词和敏感词被拒绝，设置后进入 30 天冷却。
- [ ] 日榜、周榜、总榜均可切换；前三名展示金银铜领奖台、皇冠/奖牌、渐变和轻量动画，第 4–100 名按唯一名次排列。
- [ ] 排行榜只显示昵称、四位公开编号、积分与佩戴称号，不出现内部用户 ID、OpenID、UnionID 或头像。
- [ ] “我的成就”展示 12 个徽章、条件与进度；未解锁为锁定样式，已解锁称号可佩戴、切换或取消。
- [ ] 收藏达到 30、掌握 20 道错题、首次完成 408 和 408 获得 80 分时，相关成就正常解锁且之后不回收。
- [ ] 永久删除账户后积分档案、流水和成就一并清除；重新登录使用新的空积分账户。

## C/C++ v1 数据迁移

- [ ] 准备含进度、错题、收藏和未完成会话的 `cpp_mock_state_v1`，首次启动后生成 `practice_mock_state_v2`。
- [ ] 迁移后 C/C++ 累计统计、不同题进度、错题掌握状态、收藏和活动会话与迁移前一致。
- [ ] 迁移不会影响其他六学科的初始空状态。
- [ ] 旧 v2 普通练习缺少 `scope` 时按单学科恢复；`scope=all` 的全局收藏会话恢复后学科仍为空，不会误显示为 C/C++。

## 408 客观题模拟

- [ ] 每次试卷恰好 40 道单选，ID 无重复，配比严格为数据结构 12、组成原理 12、操作系统 9、计网 7。
- [ ] 答题期间无正误、答案或解析；选项可修改，答题卡已答/未答/当前题标记正确。
- [ ] 第一至第 39 题的固定底栏始终显示“上一题、答题卡、下一题”，长题干、代码块和窄屏下无需滚到底部即可换题。
- [ ] 第一题禁用“上一题”；第 40 题将“下一题”替换为“检查并交卷”，底栏不遮挡最后一个选项并正确适配安全区。
- [ ] 选择答案后停留在当前题且允许改选；未选择答案也可以下一题，并在答题卡中保持未答标记。
- [ ] 每次选择后草稿保存；退出并重启后题号与选择恢复。
- [ ] 切后台 1 分钟再返回，剩余时间按 `expiresAt` 实际减少而非暂停。
- [ ] 提前交卷提示未答数量；未答题计错；结果总分等于正确题数乘 2。
- [ ] 时间归零自动交卷；断网时保留草稿并可重试；重复交卷不重复计分。
- [ ] 交卷后显示总分、正确率、四科表现及错题解析，考试错题进入全局错题。
- [ ] 已完成试卷从历史页只能查看，不能修改；题库修改后旧报告快照不变。

## 内容与发布

### Web 题库管理后台

- [ ] 未启用后台时 `/admin/` 返回 404；启用后台、配置私有 COS 和稳定 `ADMIN_ENCRYPTION_KEY` 后，`/admin/` 可通过当前云托管 HTTPS 域名访问，管理 API 响应带 `no-store` 且页面不能被第三方 iframe 嵌入。
- [ ] 空管理员库且存在 `ADMIN_BOOTSTRAP_TOKEN_HASH` 时，`GET /api/v1/admin/setup/status` 显示可初始化；在 `/admin/setup` 输入原始启动令牌，`prepare` 正常展示 TOTP 二维码，`complete` 校验 6 位动态码后创建唯一所有者。
- [ ] 首次建号后删除云环境中的 `ADMIN_BOOTSTRAP_TOKEN_HASH`；再次访问 `status/prepare/complete` 均返回 404，并发重复提交不能创建第二个初始所有者。
- [ ] 所有者可用密码和 TOTP 登录，刷新仍保持会话；退出后旧 Cookie 与 CSRF Token 均不能继续调用管理写接口。
- [ ] `ADMIN_REVIEW_POLICY=two-person` 时提交人无法复核自己的目录、题目和 Excel 批次；切到 `single-owner` 后只有所有者可以在查看完整差异、勾选差异/内容/警告并填写自检说明后自审通过，记录显示 `SELF_APPROVED`。
- [ ] 目录、题目和 Excel 批次提交后可由原提交人撤回；撤回后可继续编辑/重新校验，旧冻结哈希与批准不可继续用于发布，重新修改批准内容后必须再次提交自检。
- [ ] 可视化编辑器分别创建单选、多选、判断、填空和简答题；选项、答案组、参考答案、标签、408 范围和题图均可维护，预览与小程序一致；高级 JSON 只有“校验并应用”成功后才改变表单，非法 JSON 不能保存。
- [ ] Excel 上传后流程依次显示上传、校验、自检、发布；分页报告可按全部/错误/警告/通过筛选，翻页无重复或漏行，下载的 XLSX 报告包含每行原始数据和完整错误/警告。
- [ ] 发布前预览准确展示新增、修订、停用、目录变化和质量警告，返回候选 SHA-256 与确认文字；输入不同文字、过期候选或错误 TOTP 均不能发布，重新预览并输入当前 TOTP 后才能成功。
- [ ] 回滚前预览准确显示目标版本、题量和警告数；目标已成为当前版本、候选已变化或 TOTP 错误时不能回滚，成功回滚生成新的发布记录且旧发布与历史答题快照不变。
- [ ] 连续输入错误的发布/回滚 TOTP 会触发限流并最终撤销当前会话；登录、审计和运行日志中不出现密码、启动令牌、TOTP、COS 密钥或会话 Cookie。
- [ ] 在 375×812、390×844、430×932、768、1024 和 1440px 视口验收总览、题目列表/筛选、全屏编辑器、媒体上传、导入、自检、发布和回滚；页面整体无横向滚动，按钮不越界，固定操作栏不遮挡内容并适配手机安全区与软键盘。
- [ ] 350 题正式导入前先从工作簿抽取一个小批次，完成校验、自检、发布、`/health`、`/ready`、目录题量、随机练习和真机答题验证，再回滚到 500 题基线。
- [ ] 小批验证通过后导入完整 350 题，确认所有阻断错误清零、警告已逐项确认并完成自检；发布后记录快照 SHA-256，确认总题量、动态目录、运行日志和随机抽题正常，小程序无需重新上传即可读取新题。

- [x] 已按 `content/REVIEW_STATUS.md` 完成 500 题技术交叉复核并记录责任人、日期、结论和修改；详细证据见 `content/QUESTION_BANK_AUDIT_2026-07-15.md`。
- [x] 已重点核对 7 道代码题、75 道难题、53 道进阶单选及 C/C++/STL 的标准边界与实现前提。
- [ ] 确认正式 AppID、微信云托管环境/服务、公众平台隐私指引和登录流程后再提交审核；`callContainer` 模式不要求配置独立 HTTPS API 地址。
- [x] API 镜像使用非 root 用户构建，`/health` 与 `/ready` 分别通过，Compose 配置中不包含 PostgreSQL 服务。
- [x] 已在腾讯云独立数据库上执行迁移镜像、幂等题库导入、API 启动和上一镜像回滚/重新发布演练。
- [ ] “我的”、积分排行、我的成就和隐私说明页面在微信开发者工具窄屏模拟器中可正常渲染；退出和永久删除入口清晰分离。
- [x] MySQL 8 集成测试确认云身份账户可永久删除，删除后原身份请求返回 401，再次登录会创建新的业务账户。

## 2026-07-14 本地联调记录（历史，已由微信云托管方案取代）

- [x] Windows 11、本地 PostgreSQL 17：`npm run verify:all` 通过；500 题、18 页面、8 项服务端单元测试和 7 组 PostgreSQL 集成测试通过。
- [x] 微信开发者工具 Stable v2.01.2510290：真实 `wx.login/code2Session` 连续登录两次识别为同一业务用户；401 刷新和原请求重放通过，旧刷新令牌复用由集成测试确认为 401。
- [x] 微信开发者工具真实 API：创建 40 题试卷，配比为 12/12/9/7；第 1 题上一题禁用，选项保存后未自动跳转，固定下一题进入第 2 题。
- [x] 微信开发者工具真实 API：提前交卷准确提示 39 题未答，结果页显示 80 分制、四科表现、正确答案和快照解析。
- [x] 联调结束后恢复默认 Mock 模式并重新编译，未观察到项目业务编译错误。
- [x] Docker Desktop 引擎可用，`docker compose -f compose.api.yaml config --quiet` 通过，Compose 未定义 PostgreSQL 服务。
- [x] GitHub Actions 运行 `29322765212` 已完成 API/迁移镜像构建、数据库迁移、500 题导入、非 root 启动、`/health`、`/ready` 和优雅关闭；本地 Docker Hub 下载超时不再阻塞镜像门禁。
- [x] 当前正式发布门禁已切换为 MySQL 8 与微信云托管环境/服务校验；旧 PostgreSQL、预发布 HTTPS 和 `request` 合法域名不再作为本次上线前置条件。
- [x] `npm run check:release` 在云托管模式校验 `cloudEnvId`、`cloudService`、运营主体、隐私联系方式和 500 题交叉复核；仅 HTTP 模式要求 HTTPS API 地址。

## 2026-07-14 腾讯云预发布部署记录（历史，不作为正式发布或回滚基线）

- [x] Ubuntu 24.04 服务器安装 Docker Engine 29.6.1、Compose 5.3.1 与 PostgreSQL 17.10；原 3x-ui/Xray 的 443、9000、10000、2096 监听保持不变。
- [x] PostgreSQL 仅监听回环地址和 `172.28.250.0/24` 专用 Docker 网桥，API 仅绑定 `127.0.0.1:3000`，未开放新的公网端口。
- [x] 两次 Prisma 迁移成功，题库导入后数据库包含 7 学科、500 道题。
- [x] API 容器使用 `node` 非 root 用户、只读根文件系统、`cap_drop: ALL` 和 `no-new-privileges`；容器重启后 `/health`、`/ready` 均通过。
- [x] API 日志对微信 Secret、JWT 和数据库连接信息的值扫描通过，未发现敏感值回显。
- [ ] 使用 `ops/backup-mysql.sh` 生成并校验 `.sql.gz` 备份；通过 `ops/restore-mysql.sh` 恢复到一次性 MySQL 测试库，核对基线题、当前发布和历史数据后删除测试库，生产库不受影响。
- [x] 旧自建 PostgreSQL 的每日备份 timer 验证记录已归档；当前 timer 已切换为 MySQL，需在可访问云数据库的受信运维机重新演练。
- [x] 执行上一镜像回滚并通过 `/ready`，随后重新发布当前 Git SHA 镜像并再次通过就绪检查。
- [x] DNS、TLS、公网 8443 与微信 `request` 合法域名均已配置。
- [x] 该独立 PostgreSQL/HTTPS 预发布链路已停止作为小程序发布目标；体验版改为验证微信云托管链路。

## 2026-07-15 HTTPS 入口建设记录（历史，不作为微信云托管发布门禁）

- [x] `api.qushuati.cloud`、`qushuati.cloud` 和 `www.qushuati.cloud` 的 A 记录均正常解析到 `43.165.170.150`。
- [x] 在不修改 Xray 443 的前提下安装 Nginx 1.24.0，并建立 `8443 → 127.0.0.1:3000` 反向代理；80 只用于 ACME 与 HTTPS 跳转。
- [x] Let's Encrypt 证书签发成功，有效期至 2026-10-13；Certbot 定时续期已启用，续期后配置校验并重载 Nginx。
- [x] Nginx 配置通过 `nginx -t`，服务器监听 80/8443，Xray 的 443/10000 与 3x-ui 的 9000/2096 保持不变。
- [x] 腾讯云轻量应用服务器防火墙已放行 TCP 8443；公网端口连接、`/health`、`/ready`、TLS 1.2、证书链和部署验证脚本均通过。
- [x] `certbot renew --dry-run --no-random-sleep-on-renew` 模拟续期成功，续期部署钩子通过 `nginx -t` 后重载服务。
- [x] `npm run verify:all` 通过：500 题、18 页面、8 项服务端单元测试和 7 项 PostgreSQL 集成测试全部成功。
- [x] `npm run check:release` 只剩“交叉复核日期”和“复核结论”两项预期门禁，不再报告 HTTPS API 配置错误。
- [x] 微信公众平台已添加 `https://api.qushuati.cloud:8443` 为 `request` 合法域名。

## 2026-07-15 题库同步与旧开发版本上传记录（历史，需由云托管正式版覆盖）

- [x] `npm run verify:release` 全部通过：500 题、7 学科、45 章节、题型与难度配比、内容审计、18 页面、8 项服务端单元测试和 7 组 PostgreSQL 集成测试均通过。
- [x] 题库复核提交 `e0537728cf9e7922577ddeb5f6ed47fc264249c4` 已推送到 `main`，GitHub Actions 运行 `29393859076` 成功。
- [x] 腾讯云部署 `quzijie-api:e053772` 与迁移镜像；导入后数据库为 500 道当前题目，版本 1 共 96 道、版本 2 共 404 道，历史题目版本共 904 条。
- [ ] 当前云托管 MySQL 部署前已生成并校验 `quzijie-YYYYMMDDTHHMMSSZ.sql.gz`；公网 `/health`、`/ready` 和数据库就绪检查通过。
- [x] 微信开发者工具 CLI 已登录，并成功向 AppID `wx69380e593ebd5ac7` 上传开发版本 `2.1.0`，说明为“500题复核与公网API体验版”。
- [x] 上传包体积：总计 1,687,233 字节，主包 1,614,635 字节，`/modules/cpp/` 分包 72,598 字节。
- [ ] 用当前 `codex/wechat-cloudrun` 分支重新上传云托管正式版 `2.1.0`，覆盖旧 HTTP 开发版本；再在公众平台将其设为体验版并确认体验二维码与体验成员权限。
- [ ] 使用 `2.1.0` 体验版在至少一台 iOS 与一台 Android 真机完成本清单；记录设备、系统、微信版本、测试人、日期、问题截图和相关 `questionId`。
